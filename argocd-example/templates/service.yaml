apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-example.fullname" . }}
  labels:
    {{- include "argocd-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-example.selectorLabels" . | nindent 4 }}
